# Pooled commerce_runtime URL (server-only). Never give this to Payload CRM.
COMMERCE_DATABASE_URL=postgresql://USER:PASSWORD@HOST/commerce_db?sslmode=require
# Direct commerce_migrator URL, used only by controlled CI migration jobs.
COMMERCE_MIGRATION_DATABASE_URL=postgresql://USER:PASSWORD@HOST/commerce_db?sslmode=require
