{
  "name": "DogFoodApp",
  "install": "pnpm install --frozen-lockfile && pnpm db:migrate",
  "terminals": [
    {
      "name": "dev",
      "command": "pnpm dev",
      "description": "Next.js dev server (Turbopack) on http://localhost:3000"
    }
  ],
  "ports": [
    {
      "name": "web",
      "port": 3000
    }
  ]
}
