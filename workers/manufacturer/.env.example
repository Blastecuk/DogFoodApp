# Manufacturer worker — pooled least-privilege runtime role (commerce_db only).
COMMERCE_DATABASE_URL=postgresql://commerce_rt:PASSWORD@HOST/commerce_db?sslmode=require
# Production also sets RABBITMQ_URL (consume order.paid) and MANUFACTURER_API_KEY.
