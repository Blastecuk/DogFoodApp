# iii.dev API — runtime config. On Fly.io these come from `fly secrets`.
PORT=8080
FLY_PRIMARY_REGION=lhr
# Pooled commerce_runtime URL (least privilege; commerce_db only).
COMMERCE_DATABASE_URL=postgresql://commerce_runtime:PASSWORD@HOST/commerce_db?sslmode=require
